version: 2
updates:
  # SHA-pinned actions do not receive security fixes on their own. Dependabot
  # understands the pinning convention and updates both the SHA and the
  # trailing version comment, which is the part a reviewer can actually judge.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
